<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.proyectomusicatop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".decima"></activity>
        <activity android:name=".novena" />
        <activity android:name=".octava" />
        <activity android:name=".septima" />
        <activity android:name=".sexta" />
        <activity android:name=".quinta" />
        <activity android:name=".cuarta" />
        <activity android:name=".tercera" />
        <activity android:name=".segunda" />
        <activity android:name=".primera" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>